package com.hammer.app.subwaysimulator.model


//isEnabled == Falseのclassは最後列に移動する

enum class Amount{
    NONE,
    LITTLE,
    NORMAL,
    MANY,
    MAX,
}

enum class HowToDress{
    SEPARATE,
    BLEND,
}